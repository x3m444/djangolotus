from django.urls import path

websocket_urlpatterns = [
    # WebSocket consumers se adaugă aici pe măsură ce implementăm modulele
    # ex: path('ws/bucatarie/', consumers.BucatarieConsumer.as_asgi()),
]
